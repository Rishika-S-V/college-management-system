{
  "name": "flask_parcel_test",
  "version": "1.0.0",
  "description": "```python .gitkeep files are kept to make git track the directory & will be removed after adding files ```",
  "main": "index.js",
  "scripts": {
    "dev": "npx parcel ./static_src/html/*.html --no-source-maps",
    "scss-watch": "npx sass --watch static_src/scss:website/static --style=compressed  --no-source-map",
    "css-minify": "npx sass static_src/scss:website/static --style=compressed  --no-source-map",
    "js-watch": "gulp devWatch",
    "js-minify": "gulp bundleJs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-uglify": "^3.0.2",
    "parcel-bundler": "^1.12.5",
    "sass": "^1.47.0"
  }
}
